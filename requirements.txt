# Serial communication (play_pattern.py, tune_servos.py, manual_keyboard.py)
pyserial>=3.5

# YouTube → drum JSON pipeline (yt_to_json.py)
# System dependency: ffmpeg must be on PATH
yt-dlp
demucs          # drum stem separation; auto-uses CUDA on Linux, CPU on Mac
torchcodec      # required by torchaudio >= 2.11 for audio file saving
librosa
mido
soundfile
scipy
numpy
